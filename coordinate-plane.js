// Author: Khalil Tavarez

var readline = require("readline-sync");
var x1;
var x2;
var y1;
var y2;

x1 = parseInt(readline.question("What is the x1"));
y1 = parseInt(readline.question("What is the y1"));
x2 = parseInt(readline.question("What is the x2"));
y2 = parseInt(readline.question("What is the y2"));

const userFormula =  parseInt(readline.question("Would you like to find the slope of the line(1), the lenght of the line(2), or the midpoint of the line(3)?"));

//Slope: m = (y2 - y1)/(x2 - x1)
//Midpoint:
//Distance: d = sqrt((x2 - x1)^2 + (y2 - y1)^2)

if(userFormula == 1){

}
else if(userFormula == 2){

}
else {

}
